package enums


type ParentLocation int32

const (
    ParentLocationNone ParentLocation = ParentLocation(0)
    ParentLocationRightHand ParentLocation = ParentLocation(1)
    ParentLocationLeftHand ParentLocation = ParentLocation(2)
    ParentLocationShield ParentLocation = ParentLocation(3)
    ParentLocationBelt ParentLocation = ParentLocation(4)
    ParentLocationQuiver ParentLocation = ParentLocation(5)
    ParentLocationHearldry ParentLocation = ParentLocation(6)
    ParentLocationMouth ParentLocation = ParentLocation(7)
    ParentLocationLeftWeapon ParentLocation = ParentLocation(8)
    )
